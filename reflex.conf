# Reflex configuration for hot-reload development
# Watch Go files and rebuild on change

# Watch all .go files in the project
-r '\.go$' -s -- sh -c 'go build -o tmp/server ./cmd/server && ./tmp/server'
